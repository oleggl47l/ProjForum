/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PostModel } from '../../models/post-model';

export interface GetPostById$Json$Params {
  id: string;
}

export function getPostById$Json(http: HttpClient, rootUrl: string, params: GetPostById$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<PostModel>> {
  const rb = new RequestBuilder(rootUrl, getPostById$Json.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PostModel>;
    })
  );
}

getPostById$Json.PATH = '/api/v1/Post/GetPostById/{id}';
