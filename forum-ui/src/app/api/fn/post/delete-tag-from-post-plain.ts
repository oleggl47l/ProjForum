/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RemoveTagFromPostCommand } from '../../models/remove-tag-from-post-command';
import { Unit } from '../../models/unit';

export interface DeleteTagFromPost$Plain$Params {
      body?: RemoveTagFromPostCommand
}

export function deleteTagFromPost$Plain(http: HttpClient, rootUrl: string, params?: DeleteTagFromPost$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<Unit>> {
  const rb = new RequestBuilder(rootUrl, deleteTagFromPost$Plain.PATH, 'delete');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Unit>;
    })
  );
}

deleteTagFromPost$Plain.PATH = '/api/v1/Post/RemoveTagFromPost';
