/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { UpdateRoleCommand } from '../../models/update-role-command';
import { UpdateRoleResultDto } from '../../models/update-role-result-dto';

export interface UpdateRole$Json$Params {
  id: string;
      body?: UpdateRoleCommand
}

export function updateRole$Json(http: HttpClient, rootUrl: string, params: UpdateRole$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<UpdateRoleResultDto>> {
  const rb = new RequestBuilder(rootUrl, updateRole$Json.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<UpdateRoleResultDto>;
    })
  );
}

updateRole$Json.PATH = '/api/identity/Roles/{id}';
