/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CommentModel } from '../../models/comment-model';

export interface GetAllComments$Json$Params {
}

export function getAllComments$Json(http: HttpClient, rootUrl: string, params?: GetAllComments$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<CommentModel>>> {
  const rb = new RequestBuilder(rootUrl, getAllComments$Json.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<CommentModel>>;
    })
  );
}

getAllComments$Json.PATH = '/api/forum/v1/Comment/GetComments';
