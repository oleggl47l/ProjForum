/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CommentModel } from '../../models/comment-model';

export interface GetAllComments$Plain$Params {
}

export function getAllComments$Plain(http: HttpClient, rootUrl: string, params?: GetAllComments$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<CommentModel>>> {
  const rb = new RequestBuilder(rootUrl, getAllComments$Plain.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<CommentModel>>;
    })
  );
}

getAllComments$Plain.PATH = '/api/forum/v1/Comment/GetComments';
