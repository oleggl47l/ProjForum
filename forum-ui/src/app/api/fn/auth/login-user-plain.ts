/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { LoginCommand } from '../../models/login-command';
import { LoginResultDto } from '../../models/login-result-dto';

export interface LoginUser$Plain$Params {
      body?: LoginCommand
}

export function loginUser$Plain(http: HttpClient, rootUrl: string, params?: LoginUser$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<LoginResultDto>> {
  const rb = new RequestBuilder(rootUrl, loginUser$Plain.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<LoginResultDto>;
    })
  );
}

loginUser$Plain.PATH = '/api/identity/v1/Auth/login';
